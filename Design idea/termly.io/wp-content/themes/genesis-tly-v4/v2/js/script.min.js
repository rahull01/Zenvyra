function openModal(e, t) {
    e.preventDefault();
    let l = t.getAttribute("data-modal"),
        a = document.getElementById(l);
    a.showModal();
    let s = a.getElementsByTagName("iframe")[0],
        v = t.getAttribute("data-video-url");
    if (s) {
        let o = s.getAttribute("data-src") || "";
        v && (o = v), o && o.includes("youtube") && (s.setAttribute("src", o), a.addEventListener("close", e => {
            s.setAttribute("src", "")
        }))
    }
}! function() {
    let e = e => {
            e.forEach(e => {
                e.isIntersecting ? e.target.classList.add("inview") : e.target.classList.remove("inview")
            })
        },
        t = new IntersectionObserver(e, {
            rootMargin: "0px 0px 0px 0px",
            threshold: .2
        }),
        l = document.querySelectorAll(".anim");
    l.forEach(e => {
        t.observe(e)
    });
    let a = document.querySelectorAll(".feature-accordion-item"),
        s = document.querySelector(".feature-images"),
        o = document.querySelectorAll(".feature-image");
    a.forEach(e => {
        let t = e.getAttribute("data-feature"),
            l = e.getAttribute("data-switch");
        e.addEventListener("click", c => {
            console.log('[data-id="' + t + '"]'), a.forEach(e => {
                e.classList.remove("active")
            }), o && "false" !== l && o.forEach(e => {
                e.classList.add("hidden"), e.classList.remove("block")
            }), e.classList.add("active");
            let r = s.querySelector('[data-feature="' + t + '"]');
            console.log(r), o && "false" !== l && (r.classList.remove("hidden"), r.classList.add("block"))
        })
    });
    let c = document.querySelectorAll(".tab-item"),
        r = document.querySelector(".tab-content-wrap"),
        n = document.querySelectorAll(".tab-content");
    c.forEach(e => {
        let t = e.getAttribute("data-tab");
        e.addEventListener("click", l => {
            c.forEach(e => {
                e.classList.remove("active")
            }), n && n.forEach(e => {
                e.classList.remove("active")
            }), e.classList.add("active");
            let a = r.querySelector('[data-tab="' + t + '"]');
            a && a.classList.add("active")
        })
    });
    let d = document.querySelectorAll(".accordion");
    d.forEach(e => {
        let t = e.querySelectorAll("section");
        t.forEach(e => {
            e.addEventListener("click", l => {
                e.classList.contains("active") ? e.classList.remove("active") : (t.forEach(e => {
                    e.classList.remove("active")
                }), e.classList.add("active"))
            })
        })
    });
    let u = document.querySelectorAll(".mobile-dropdown");
    u.forEach(e => {
        let t = e.getElementsByClassName("mobile-dropdown-show")[0],
            l = e.getElementsByClassName("subnav")[0],
            a = e.getElementsByClassName("nav-chev")[0];
        t.addEventListener("click", t => {
            l.classList.toggle("hidden"), e.classList.toggle("active"), a.classList.toggle("rotate-90")
        })
    });
    let g = document.getElementById("nav-mobile");
    document.getElementById("nav-items");
    let v = document.querySelectorAll(".burger"),
        f = document.getElementsByTagName("body")[0];
    v.forEach(e => {
        e.addEventListener("click", function() {
            this.classList.toggle("close"), g.classList.toggle("left-full"), g.classList.toggle("left-0"), g.classList.toggle("opacity-0"), g.classList.toggle("pointer-events-none"), f.classList.toggle("overflow-hidden"), f.classList.toggle("h-full")
        })
    })
}();
const carousel = document.querySelector(".carousel-scroll-container");
if (carousel) {
    let e = carousel.querySelector(".carousel-media"),
        t = carousel.querySelector(".carousel-control.next"),
        l = carousel.querySelector(".carousel-control.prev"),
        a = e.querySelector(".carousel-item").clientWidth;

    function s() {
        e.scrollBy({
            left: a,
            top: 0,
            behavior: "smooth"
        })
    }

    function o() {
        e.scrollBy({
            left: -a,
            top: 0,
            behavior: "smooth"
        })
    }
    t.addEventListener("click", s), l.addEventListener("click", o)
}
window.init = function() {
    console.log("INIT"), window.tly_data && window.tly_data.categories && -1 !== window.tly_data.categories.indexOf("performance") && window.replaceVideos()
}, window.tly_loaded ? init() : window.addEventListener("tly_loaded", init), window.replaceVideos = function() {
    let e = document.querySelectorAll(".video"),
        t = document.querySelectorAll(".delayed");
    e.forEach(e => {
        let t = e.nextElementSibling;
        ("img" == t.tagName.toLowerCase() || "svg" == t.tagName.toLowerCase()) && t.classList.contains("replace") && t.classList.add("hidden"), e.classList.remove("hidden")
    }), t.forEach(e => {
        let t = e.getAttribute("data-tly_src");
        t ? e.setAttribute("src", t) : e.classList.add("hidden")
    })
};
const container_els = document.querySelectorAll(".modular-dropdown");
if (0 !== container_els.length)
    for (let i = 0; i < container_els.length; i++) new Toggle(container_els[i]);

function Toggle(e, t) {
    let l = !1,
        a = e.querySelector(".btn");

    function s() {
        l = !1, e.classList.remove("open"), document.removeEventListener("keyup", c), document.removeEventListener("click", o)
    }

    function o(t) {
        !e.contains(t.target) && s()
    }

    function c(e) {
        27 === e.keyCode && s()
    }
    a && a.addEventListener("click", function a() {
        if (l) {
            s();
            return
        }
        if (l = !0, e.classList.add("open"), document.addEventListener("keyup", c), document.addEventListener("click", o), t) {
            let r = e.querySelector("input");
            r && r.focus()
        }
    })
}